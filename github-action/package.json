{
  "name": "github-action",
  "type": "module",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "svg-pacman-contributions": "^1.0.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "rimraf": "^6.0.1"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "pnpm run clean && ncc build src/index.js -o dist"
  }
}